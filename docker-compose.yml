version: '3.8'

services:
  waha:
    image: devlikeapro/waha
    ports:
      - "3000:3000"
    volumes:
      - waha_data:/app/data
    env_file:
      - .env
    stdin_open: true
    tty: true

volumes:
  waha_data:
